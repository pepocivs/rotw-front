{
  "name": "recipes-of-the-wild",
  "version": "1.4.0",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "eslint": "^3.12.0",
    "eslint-config-google": "^0.7.1",
    "eslint-plugin-html": "^1.7.0",
    "gulp": "^4.0.2",
    "gulp-replace": "^0.5.4",
    "gulp-run": "^1.7.1",
    "jetifier": "^2.0.0",
    "jshint": "^2.9.4"
  },
  "scripts": {
    "lint": "eslint . --ext js,html",
    "test": "npm run lint && polymer test",
    "build:android": "polymer build && npx cap sync",
    "android:open": "npx cap sync && npx cap open android",
    "deploy": "polymer build && firebase deploy",
    "assets:generate": "npx capacitor-assets generate"
  },
  "dependencies": {
    "@capacitor/android": "^2.5.0",
    "@capacitor/assets": "^3.0.1",
    "@capacitor/cli": "^2.5.0",
    "@capacitor/core": "^2.5.0"
  }
}
